pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
            dir 'jenkins/Docker'
        }
    }
    stages {
        stage('Testing') { 
            steps {
                sh 'npm install'
            }
        }
    }
}